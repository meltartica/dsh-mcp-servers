# Framework packages span two rc lines (rc.1 framework vs rc.6 client primitives)
# with diverging @deepseek-ai/dsh-invariants peers. All are build-time types;
# runtime copies come from the host, so strict peer resolution is unnecessary.
legacy-peer-deps=true
